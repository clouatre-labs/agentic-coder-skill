Client-facing deliverables: recommendation documents, roadmaps, and presentations.

Filename convention: `YYYY-MM-DD-slug.md`

Commit DOCX alongside the markdown source for all delivery documents.
Diagrams go in `images/`; commit both the source file (`.mmd` or `.dot`) and the rendered `.png`.
